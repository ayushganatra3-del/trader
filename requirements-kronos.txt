# Optional extras for the Kronos forecasting strategy (agent/kronos).
# Pins follow https://github.com/shiyu-coder/Kronos/blob/master/requirements.txt.
# For a CPU-only machine, install torch first from the CPU index to avoid the
# multi-GB CUDA wheels:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0
einops==0.8.1
huggingface_hub==0.33.1
safetensors==0.6.2
tqdm
