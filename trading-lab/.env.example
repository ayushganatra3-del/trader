# PAPER credentials only. Never commit real credentials or paste them into chat.
APCA_API_KEY_ID=
APCA_API_SECRET_KEY=
PAPER_STRATEGY=trend
PAPER_SYMBOL=SPY
# The journal must be on persistent local storage. Do not delete to clear a halt.
PAPER_STATE_DIR=.paper-state
