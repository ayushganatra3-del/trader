export type ContextTool={name:string;description:string;inputSchema:object;annotations:{readOnlyHint:boolean;untrustedContentHint:boolean};execute:(input:unknown)=>unknown};
export function researchTool(run:(strategy:string)=>unknown):ContextTool{return {name:'run_strategy_backtest',description:'Run a hypothetical backtest on the currently loaded research dataset, using the current assumptions and evaluation window. Updates the selected strategy and results. Does not place any order.',inputSchema:{type:'object',properties:{strategy:{type:'string',enum:['hold','trend','momentum','breakout','rsi']}},required:['strategy'],additionalProperties:false},annotations:{readOnlyHint:false,untrustedContentHint:true},execute(input:unknown){if(!input||typeof input!=='object'||Object.keys(input).some(k=>k!=='strategy')||!('strategy' in input)||!['hold','trend','momentum','breakout','rsi'].includes(String(input.strategy)))throw Error('Choose one supported strategy.');return run(String(input.strategy));}};}
